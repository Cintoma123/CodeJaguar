# Runtime dependencies for the CodeJaguar backend.
#
# Kept in sync with pyproject.toml. The CLI installs these into an isolated
# virtualenv at ~/.jaguar/venv on first run (see cli/src/services/setup.ts),
# so a clean machine with only Python + Node can run `jaguar review`.
fastapi>=0.136.3
httpx>=0.28.1
keyring>=25.7.0
pydantic>=2.13.4
uvicorn>=0.49.0